<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springex1.mapper.TodoMapper">

    <insert id="insert">
        insert into tb1_todo (title, dueDate, writer)
        values (#{title}, #{dueDate}, #{writer})
    </insert>


    <select id="selectAll" resultType="com.example.springex1.domain.TodoVO">
        select * from tb1_todo order by tno desc
    </select>

    <select id="selectOne" resultType="com.example.springex1.domain.TodoVO">
        select * from tb1_todo where tno = #{tno}
    </select>

    <delete id="delete">
        delete from tb1_todo where tno = #{tno}
    </delete>

    <update id="update">
        update tb1_todo set title = #{title}
        ,dueDate = #{dueDate}
        ,finished = #{finished}
        where tno = #{tno}
    </update>

</mapper>